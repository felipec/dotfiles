color normal white black
color tree cyan black

# color markers brightred black

color header green black ^(from|envelope-from|subject|to|envelope-to|date|cc|bcc):
color header yellow black ^subject:
color quoted blue black
color status yellow black
color signature yellow black

# color header brightred black subject
# color hdrdefault brightwhite black
# color status black cyan
# color indicator default green
# color indicator default white

# color normal default default
# color indicator black magenta
# color hdrdefault white default
# color attachment white default

# color index blue black ~A # all
# color index green black ~O # old # never happen?
color index default default ~R # read
color index brightwhite black "~p" # to me
color index cyan black ~l # cyan
color index brightblue default '%f bugs'
color index green default '%f mma'
color index brightyellow black ~U # unread
color index brightyellow black ~N # new
color index brightmagenta black ~F # flagged
color index blue black ~T # tagged
color index red black ~D # deleted

# vim: set ft=muttrc:
